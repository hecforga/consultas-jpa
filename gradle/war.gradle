apply plugin: "war"

bootWar {
    mainClassName = "com.nunsys.consultas.ConsultasApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
